function namesToUpperCase(names) {
    const counter = 5;
    while (counter > 0) {
        counter--;
    }
    return namesToUpperCase();
}
const names = ["adam", "eve", "steve"];
console.log(namesToUpperCase(names)); 